{
  "$defs": {
    "assay_enum": {
      "enum": [
        "RNA-seq",
        "scRNA-seq",
        "DNA-seq",
        "ATAC-seq"
      ],
      "title": "assay_enum",
      "type": "string"
    },
    "experimentalSystems_enum": {
      "enum": [
        "Cell Line",
        "Patient Sample"
      ],
      "title": "experimentalSystems_enum",
      "type": "string"
    },
    "scope_enum": {
      "enum": [
        "private",
        "shared",
        "public"
      ],
      "title": "scope_enum",
      "type": "string"
    }
  },
  "description": "This document records the details of the SDP dataset metadata content.",
  "properties": {
    "assay": {
      "allOf": [
        {
          "$ref": "#/$defs/assay_enum"
        }
      ],
      "description": "The assay performed on the samples in the dataset to measure changes in or ask questions about the experimental system.",
      "title": "Readout Assay"
    },
    "description": {
      "description": "Please include a description or any other helpful comments or annotations for the dataset.",
      "title": "Dataset Description",
      "type": "string"
    },
    "experimentalSystems": {
      "description": "The type of biological/biochemical material(s) used as the subject(s) of the readout assay. Please select all that apply, considering all samples in the dataset. This selection will expand in future versions of this form. If you do not see your system represented here, feel free to leave this field blank.",
      "items": {
        "$ref": "#/$defs/experimentalSystems_enum"
      },
      "title": "Experimental System",
      "type": "array",
      "uniqueItems": true
    },
    "labName": {
      "description": "Please enter the name of the lab to which the dataset belongs.",
      "title": "Lab Name",
      "type": "string"
    },
    "name": {
      "description": "Please provide a unique name for the dataset.",
      "title": "Dataset Name",
      "type": "string"
    },
    "scope": {
      "allOf": [
        {
          "$ref": "#/$defs/scope_enum"
        }
      ],
      "description": "The privacy level of the dataset.",
      "title": "Scope"
    }
  },
  "required": [
    "assay",
    "description",
    "name",
    "scope"
  ],
  "title": "Dataset",
  "type": "object"
}
