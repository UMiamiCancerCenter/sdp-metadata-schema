{
  "title": "SDP Dataset",
  "description": "This document records the details of the SDP dataset metadata content",
  "version": "0.0.2",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "title": "Dataset Name"
    },
    "labName": {
      "type": "string",
      "title": "Lab Name"
    },
    "description": {
      "type": "string",
      "title": "Description"
    },
    "scope": {
      "title": "Scope",
      "description": "The privacy level of the dataset",
      "type": "string",
      "enum": [
        "private",
        "shared",
        "public"
      ],
      "default": "private"
    },
    "assay": {
      "title": "Readout Assay",
      "description": "Supported readout assays: RNA-seq, scRNA-seq, DNA-seq, ATAC-seq",
      "type": "string",
      "enum": [
        "RNA-seq",
        "scRNA-seq",
        "DNA-seq",
        "ATAC-seq"
      ],
      "default": "RNA-seq"
    },
    "experimentalSystem": {
      "type": "array",
      "description": "The cell, tissue, or other biological/biochemical material used as the subject of the experiment.",
      "title": "Experimental System",
      "items":{
        "type": "string",
        "enum": [
          "Cell Line",
          "Patient Sample"
        ],
        "default": "Cell Line"
      },
      "uniqueItems": true
    }
  },
  "required": [
    "name",
    "labName",
    "description"
  ]
}